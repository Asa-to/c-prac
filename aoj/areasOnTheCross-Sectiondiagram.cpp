#include <bits/stdc++.h>

using namespace std;

/*
* 水たまりができそうな時に深さを持ちながら適当にやってく?みたいな感じでなんとかなるのかなぁ。
*/
int main(void){
    string data;
    cin >> data;
    stack<int> ans;
    for(auto it = data.begin(); it != data.end(); it++){
       if(*it == '\\'){
       }
    }
}